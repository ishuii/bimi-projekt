Ablauf Git 

# Synchroniseren => Zyklus Symbol = Pull (Update)
# source control Repo => Pull 

#Branche erstellen Stgr command  P => Create Branch 

#Stagen = + Symbol anklicken => Änderungen die mit in den Commit sollen 

# Commiten => Button Commit dafür eine klare Message eintragen, was durchgeführt wurde

# Publish Branch on GitHub => push request => wohin? Origin 

# auf GitHub gehen und Pull Request 

# "Compare & Pull Request" → darauf klicken. 
#Falls es nicht angezeigt wird, suchen im Pull Request Tab → dann auf "New Pull Request"
das einstellen 
#base: dev
compare: feature/mein Branch

#danach seinen Branch löschen 

#in VS Code Pull in DEV!
