## Jour Fixe 10.04.2026

### Team GUI

#### To Dos

### Team Visualisierung

#### To Dos

### Team Clusterverfahren
- eigene Funktion für Distanzmatrix ist fertig
  - darf die stats-Funktion dist verwendet werden? (ist in C geschrieben und wäre deutlich schneller)
  - sollen weitere Distanzfunktionen angeboten werden? Abfrage in der GUI?
- Single Linkage Funktion geschrieben
  - produziert ein Ergebnis, aber noch nicht richtig getestet &rarr; unklar, ob es korrekt ist
  - liefert zwei Werte für die Visualisierung: matched_at (Vektor der Minima, an denen Cluster gebildet worden waren) und cluster_history (eine Liste aus Listen von Vektoren, die den Ablauf der Clusterung abbilden)
- Datennormierung - wie durchzuführen? Soll der Nutzer auswählen sollen, ob die Daten bereits normiert sind? Wird grundsätzlich immer normiert?

- wie sollen wir im Team die Aufgaben verteilen? Wer von uns macht welchen Teil ? Wenn fast alles schon fertig ist, was bleibt wirklich noch übrig?? :( Es soll ja von jedem ersichtlich sein, was er beigetragen hat vor allem bezogen auf die Bewertung
- https://rpubs.com/aaronsc32/hierarchical-clustering-single-linkage-algorithm
- Definition was ist überhaupt Semantische Datenintegration? 
"Semantische Datenintegration ist das Zusammenführen von Daten aus unterschiedlichen, heterogenen Quellen"
 Unser Fall: Messdaten Labor & KEGG/GO ==> diese connecten 
- Daten einlesen => GUI:  Auswahl gewünschter Gruppe = Summe von Features, die bestimmter Gruppe angehören (z.B. Glykolyse) ==> Mapping Datei:  
===> deutlich reduzierter Datensatz mit n verschiedenen Genen 
  - Mithilfe von REST API Zugriff: Gene ID und Name: 
    https://rest.kegg.jp/list/hsa
  - Mithilfe von REST API Zugriff: Pathway Name & PATHWAY ID: 
  https://rest.kegg.jp/list/pathway/hsa
  - Mithilfe von REST API Zugriff: Gene für Pathway ....
  https://rest.kegg.jp/link/hsa/hsa01100 für Pathway hsa01100
  - Mithilfe von REST API Zugriff: Beschreibung für GEN Homo Sapiens 2997 
  https://rest.kegg.jp/get/hsa:2997

- Gendatensatz nur von Menschen???
- Pathways mit Genen irgendwo persistent speichern? ==> SQLite??


#### To Dos
- Single Linkage testen &rarr; liefert was es soll?
- Verbindung von Dateineinlesen (Team GUI) und Normierung (Team Cluster) bauen (bislang lesen wir die Daten selber ein)
- Datennormierung auslagern

- Daten einlesen geht ja mit unserer Feature selection einher = eigentlich müssten wir uns auch um das einlesen kümmern? 
- CSV => Trennzeichen, welches Format erlauben wir, wie wird das gesteuert in R? 
- Datensatz verstehen, was bedeutet das alles???? 
- ausgewählte Gene sind als Zahl dargestellt bzw als ID, ist das in allen Datensätzen so oder muss man diese 'übersetzen' Stichwort Genname vs Entrez ID ==> festlegen welches Format wir verwenden
- über NCBI 


### Generelle Anliegen
- Code-Regeln (Sprache, Dateibenennung, etc.) besprechen (siehe docs/implementation.md)
- Deadline für große Abgabe besprechen
- Wer führt Tests und die Endanpassung durch?
### Generelle To Dos
