## Jour Fixe 24.04.2026

### Team GUI

#### To-Dos


### Team Visualisierung

#### To-Dos


### Team Cluster
- Distanzfunktion
  - ausgelagert und in C++ umgeschrieben für höhere Effizienz (microbenchmark für Vergleich)
  - Minkowski-Distanz hinzugefügt (Team GUI muss Eingabe eines Parameters ermöglichen)
- Auslagerung der Funktionstests in eigene Dateien für bessere Übersicht &rarr; sinnvoll für alle Gruppen, um unnötigen Overhead / fehlerhafte Überschreibungen zu vermeiden, wenn auf andere Skripte zugegriffen wird
- Single Linkage
  - umgeschrieben, gibt jetzt neben dem matched_at-Vektor auch die Merge-Matrix zurück
  - Merge Matrix stimmt mit der von hclust inhaltlich überein, allerdings andere Reihenfolgen pro Zeile &rarr; Probleme bei der Anordnung?

- Datenbank ist aufgesetzt und fix, ERM Modell dazu im data Ordner
  - ein Skript für Setup Database
  - weitere Skript mit Funktionen, die auf die Datenbank zugreifen 
    - Datensatz "preprocessen", Daten und Label trennen, Spalte umbenennen
    - Rückgabe von Genen, die zum ausgewählten Pathway gehören 
    - Preprocessed Datensatz Abgleich mit den zurückgegebenen Genen ==> dezimierter Datensatz

#### To-Dos
- weitere Distanzmaße hinzufügen: Canberra, Pearson, Angular
- weitere Linkage-Funktionen

- Datensätze immer im selben Format??? Dann klappt es sonst schwierig, sehr viele Fehlerfälle möglich
- Gene können nicht als Row names eingelesen werden, da potentiell doppelt oder mehrfach 
- aktuell 1. Spalte ==> Gene ==> Benennung nach der Spalte in der Datenbank 
- Was macht man mit den Genen, die trotzdem doppelt im dezimierten Datensatz vorkommen???


### Generelle Anliegen
