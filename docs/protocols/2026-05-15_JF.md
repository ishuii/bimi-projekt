## Jour Fixe 15.05.2026

### Generell
- unter docs/bericht Tex-Dateien angelegt für den Bericht
- jeder arbeitet in seinem eigenen Ordner, wird am Ende zusammengefügt in main.tex


### Team GUI
- Pathways eingebaut
- Info box/ Pop-up für Farbpaletten eingebaut
- Pop-up beim Zurück-Button eingebaut
  
#### TO DO
- Header fixen
- Schrift schwarz machen
- Hover-Funktion bei Heatmaps
- Speicher klären
  - temporärer Speicher für Einstellungen (Cache), z.B. wenn man einfach nur das Clusterverfahren ändern will, aber die Distanzmatrix gleich bleibt &rarr; Distanzmatrix müsste dann eingespeichert werden
- File input muss geändert werden damit es Dateien größer als 5MB erlaubt
- Parameters in sidebar implemetieren
- Pop-up implementieren nach Hochladen von Datensatz wenn da NA Werte drin sind
  

### Team Visualisierung


### Team Cluster
- alle Linkage-Funktionen zusammengefügt zu einer Funktion hierarchical_clustering
  - erlaubt nun auch eine "Custom" Funktion, die von Team GUI eine Abfrage von vier Parametern erfordert
  - vielleicht auch hier eine Infobox, denn obwohl fast alles berechnet werden kann, liefern viele Werte keine sinnvolle Clusterung
  - nimmt bis jetzt nur rohe Werte &rarr; man könnte vielleicht auch Eingabe von size_a und size_b erlauben? Noch nicht ganz klar
  - drei Hauptfunktionen getestet, funktionieren (custom auch getestet, funktioniert grundsätzlich mit "normalen" Parametern)
 

 - Funktion implementiert, die erkennt, ob es Gennamen sind oder IDs und dementsprechend den Datensatz weiterverarbeitet ==> sollte klappen
 - Datensatz CSV erstellt mit Random Gennamen ==> Mail ==> Frage Herr Lausser
 - Funktion eingebaut, die eine Liste von IDs und zugehörigen Namen ausgibt, welche der Gene nicht im Datensatz vorgekommen sind aber dem Pathway zugehörig sind 
 - "roher" Datensatz muss zwingend einem bestimmten entsprechen:
    - 1. Zeile = IDs oder Namen 
    - Meta Daten müssen mit Meta_ anfangen!! Reihenfolgen ist egal, aber Präfix muss stimmen

- prepare_data function untersucht jetzt den datensatz(ohne labels) auf is.empty, genug zeilen, ob er NA-Werte hat
- wandelt dataframe in matrix um in numerische Werte, checkt nochmal auf NA-Werte und Infinite Werte

- noch zu tun: 
  - Doku vervollständigen 
  - Testcases überlegen ==> kann GUI auch run_data_integration aufrufen?
  - generell Fehlerfälle überlegen 
  - Ausmachen wie wir NA in datensatz behandeln wolln und wie sind Infinites original im Datensatz eingetragen? (als String würde er bei numerisch zu NA werden)
  
