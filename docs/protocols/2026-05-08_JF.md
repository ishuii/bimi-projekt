## Jour Fixe 08.05.2026

### Generell
- renv hinzugefügt
  - jeder muss einmal ausführen: ```install.packages("renv")``` und ```renv::restore()```
  - wenn ein neues Package hinzugefügt wird, ```renv::snapshot()``` und dann renv.lock committen
  - nach einem Pull ```renv::restore()```, v.a. wenn jemand anderes ein neues Package hinzugefügt hat (= Veränderung in der renv.lock Datei)
  - mehr Infos: https://rstudio.github.io/renv/articles/renv.html
 


### Team GUI

- Verbindung zwischen UI und Backend-Funktionen eingebindet
- Fehlermeldungen für Minkowski-Distanz eingebildet
- Preset speichern und Preset laden fertig gestellt
- Ui Button für Pdf export erstellt, Backend ist noch bugged


#### To-Dos



### Team Visualisierung
- Erste Implementierung der Funktionen, die für die Visualisierung des Dendrogramms benötigt werden
    - calculate_coords
    - draw_segments
    - plot_dendro

#### To-Dos
- Dendrogramm-Funktionen an ggplot anpassen
- Labels einpflegen
- Heatmap und Dandrogramm zusammenführen
- Farbpaletten testen
- Kombiniertes Grafikpanel testen


### Team Cluster
- dist_cpp-Funktion in eigenes Package (distRcpp) ausgelagert
  - Installation und Aufruf getestet, sollte jetzt funktionieren
 
- Average und Complete Linkage getestet und funktional

- Funktion implementiert, die den gefilterten Datensatz zurückgibt mit den IDs als Rownames, Meta Daten als Dataframe, ID Vector der Gene, Gene Namen 
- Um die richtige process data Funktion auswählen zu können, muss in der GUI angegeben werden, ob es sich um IDs handelt oder Gennamen ==> Absprache mit Alisa
- CSV Datei mit Metadaten bearbeitet und Funktion dafür geschrieben für Age und Gender
- Funktion für verschiedene Datensätze getestet => klappt eigentlich 



#### To-Dos
- Fehlerbehandlung in Linkage Funktionen hinzufügen:
- Funktion schreiben, die zurückgibt welche Gene, die zu Pathway gehören evtl nicht im Datensatz sind 
- allgemeine Fehlerbehandlung von Funktionen 
- Connection Datenbank schließen, wie und wann? 
- Meta Daten filtern ==> sucht GENAU nach Meta_labels ==> sprich es muss genauso heißen, schlecht??
- Absprache mit Team GUI
 
